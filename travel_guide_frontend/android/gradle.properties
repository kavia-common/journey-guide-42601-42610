# Build performance optimizations
org.gradle.jvmargs=-Xmx4g -XX:+UseG1GC -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8 -XX:+UseStringDeduplication
org.gradle.daemon=false
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.workers.max=6
org.gradle.vfs.watch=false

# Kotlin optimizations
kotlin.compiler.execution.strategy=in-process
kotlin.incremental=true
kotlin.incremental.android=true

# Android optimizations
android.useAndroidX=true
android.enableJetifier=true
android.nonTransitiveRClass=true
android.nonFinalResIds=true
android.debug.testCoverageEnabled=false

# Force a consistent NDK version to match the Android Gradle Plugin requirements
# This resolves mismatches like configured 26.x vs required 27.0.12077973
android.ndkVersion=27.0.12077973